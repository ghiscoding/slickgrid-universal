import type { PinningOption } from './docking.interface.js';

/** Current permanent pinning state used by GridState presets and change events. */
export interface CurrentPinning extends PinningOption {}
