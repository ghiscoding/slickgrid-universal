import * as services from '../index';

describe('Testing Vanilla Services entry point', () => {
  it('should have multiple service entries defined', () => {
    expect(services).toBeTruthy();
  });
});
