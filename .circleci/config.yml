version: 2
experimental:
  pipelines: true
jobs:
  build:
    working_directory: ~/slickgrid-universal
    docker:
      - image: circleci/node:13-browsers
    steps:
      - checkout
      - restore_cache:
          key: slickgrid-universal-build-{{ .Branch }}-{{ checksum "package.json" }}
      - run: yarn install
      - save_cache:
          key: slickgrid-universal-build-{{ .Branch }}-{{ checksum "package.json" }}
          paths:
            - "node_modules"
      - run:
          name: TypeScript Build (tsc)
          command: yarn run build
      - run:
          name: Web Prod Build
          command: |
            npm run bootstrap
            cd packages/web-demo-vanilla-bundle
            npm run build:demo
      - run:
          name: Run Jest tests with JUnit as reporter
          command: ./node_modules/.bin/jest --config test/jest.config.js --ci --runInBand --collectCoverage=true --reporters=default --reporters=jest-junit
          environment:
            JEST_JUNIT_OUTPUT: "reports/junit/js-test-results.xml"
      - run:
          name: Upload Jest coverage to Codecov
          command: bash <(curl -s https://codecov.io/bash)

      - run:
          name: Run Web Server
          command: |
            cd packages/web-demo-vanilla-bundle
            npm run serve
          background: true
      - run:
          name: Running Cypress E2E tests with JUnit reporter
          command: |
            yarn run cypress:ci:xml
      - run:
          name: Full Bundle for all Bundler types
          command: yarn run bundle
